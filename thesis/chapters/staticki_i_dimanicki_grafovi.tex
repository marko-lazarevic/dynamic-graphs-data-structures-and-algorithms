% Статички и динамички графови

Како бисмо могли прецизније да дефинишемо динамичке графове, прво ћемо дати стандардну математичку дефиницију графа, а затим ћемо се фокусирати на динамичке графове, њихове карактеристике и разлике у односу на стандардне графове.

\section{Граф}

\textbf{Граф} $G$ је уређени пар $(V,E)$ који се састоји од скупа $V$ чије елементе називамо \emph{чворовима}, и скупа $E$ чији су елементи парови чворова из $V$, које називамо \emph{гранама} графа. Кажемо да грана спаја два чвора којима је одређена, а за чворове те гране кажемо да су суседни. За грану која спаја два чвора $u$ и $v$ кажемо да је инцидентна са чворовима $u$ и $v$, а чворове $u$ и $v$ називамо крајевима те гране. За гране инцидентне са истим чвором кажемо да су суседне  \cite{Stanic2020}. 

\begin{example}
    \label{ex:primer_neusmerenog_grafa}
    На слици~\ref{fig:primer_grafa} је приказан граф чији су чворови елементи скупа $V = \{A,B,C,D,E\}$, а гране елементи скупа $E = \{(A,B),\allowbreak (A,C),\allowbreak (B,C),\allowbreak (C,D),\allowbreak (C,E),\allowbreak (E,A)\}$.
\end{example} 

\begin{figure}[ht]
\centering
\begingroup
\shorthandoff{"}
\digraph[scale=0.6]{graphexample}{
    rankdir=LR;
    edge [dir=none, color=black];
    node [shape=circle];
    A -> B;
    A -> C;
    B -> C;
    C -> D;
    C -> E;
    E -> A;
}
\endgroup
\caption{Неусмерени нетежински граф са пет чворова и шест грана}
\label{fig:primer_grafa}
\end{figure}

Уколико гранама доделимо усмерење такав граф називамо \textbf{усмерени граф}, а у супротном за граф кажемо да је \textbf{неусмерен}. Графу $G=(V,E)$ можемо доделити функцију $g: E \to \mathbb{R}$, која свакој грани придружује реалан број који називамо \textbf{тежином} дате гране. На тај начин добијамо \textbf{тежински граф} $G'=(V,E,g)$. Граф чије гране немају тежину називамо \textbf{нетежинским графом} \cite{Stanic2020}. Неусмерени графови се могу представити помоћу усмерених заменом сваке неусмерене гране $\{u,v\}$ са две усмерене гране $(u,v)$ и $(v,u)$. Нетежински графови се могу посматрати као тежински графови чије гране имају једнаку тежину (најчешће 1). 

\begin{example}
\label{ex:primer_usmerenog_grafa}
У примеру \ref{ex:primer_neusmerenog_grafa}, усмерење грана није назначено, али ако узмемо да су гране усмерене од првог ка другом чвору у пару, тада је, на пример, грана $(A,B)$ усмерена од $A$ ка $B$. Уколико би се сматрало да су гране неусмерене, онда би грана $(A,B)$ била и усмерена од $A$ ка $B$ и од $B$ ка $A$. Такође графу G можемо доделити и функцију 

\[
g:\quad (A,B) \mapsto 3,\; (A,C) \mapsto 2,\; (B,C) \mapsto 1,\; (C,D) \mapsto 4,\; (C,E) \mapsto 5,\; (E,A) \mapsto 6
\]

која гранама придружује тежине,чиме добијамо тежински граф. На слици \ref{fig:primer_usmerenog_grafa} је дат пример усмереног тежинског графа са пет чворова и шест грана.
\end{example}

\begin{figure}[ht]
\centering
\begingroup
\shorthandoff{"}
\digraph[scale=0.6]{dirgraphexample}{
    rankdir=LR;
    edge [color=black];
    node [shape=circle];
    A -> B [label=3];
    A -> C [label=2];
    B -> C [label=1];
    C -> D [label=4];
    C -> E [label=5];
    E -> A [label=6];
}
\endgroup
\caption{Усмерени тежински граф са пет чворова и шест грана}
\label{fig:primer_usmerenog_grafa}
\end{figure}

Појам \textbf{степена чвора} представља једно од основних својстава чворова у графу. У неусмереном графу, степен чвора представља број грана инцидентних са тим чвором. У усмереном графу разликују се улазни степен (број грана које улазе у чвор) и излазни степен (број грана које излазе из чвора) \cite{Stanic2020}.

\textbf{Редак граф} (енгл. \textit{sparse graph}) је граф који има значајно мање грана од максималног броја грана који би могао да има. Специјални случај ретких графова су они графови чији је број суседа чворова ограничен неком (малом) константом \cite{vandergrinten_et_al:LIPIcs.SEA.2022.11}. 

У даљем тексту подразумеваћемо да се ради о усмереним, тежинским, ретким графовима, осим ако није другачије назначено.  

\section{Динамички граф}

Према стандардној дефиницији графа, граф је статички објекат који се не мења током времена. По тој дефиницији, свака промена у структури графа би значила да се ради о новом графу. Тако је на пример граф $G'=(V',E')$ (слика \ref{fig:primer_grafa2}) добијен уклањањем гране $(C,E)$ из графа $G=(V,E)$ различит граф од $G$, јер су им скупови грана различити. Како бисмо ово превазишли, потребан нам је модел која ће омогућити праћење промена у графу током времена. Такав модел називамо \textbf{динамички граф}.

\begin{figure}[ht]
\centering
\begingroup
\shorthandoff{"}
\digraph[scale=0.6]{graphexample2}{
    rankdir=LR;
    edge [dir=none, color=black];
    node [shape=circle];
    A -> B;
    A -> C;
    B -> C;
    C -> D;
    C -> E [style=invis];
    E -> A;
}
\endgroup
\caption{Неусмерен граф са пет чворова и уклоњеном граном (C,E)}
\label{fig:primer_grafa2}
\end{figure}


\subsection{Врсте динамичких графова}

Динамичке графове можемо поделити на основу промена које се дешавају у њима. За тежински граф $G=(V,E,g)$, елементи који се могу мењати током времена су: скуп чворова $V$, скуп грана $E$ и функција $g$ која гранама придружује тежине. На основу тога, можемо издвојити следеће врсте динамичких графова \cite{HARARY199779}:

\begin{itemize}
    \item \textbf{Чвор-динамички граф} (енгл. \textit{vertex-dynamic graph}) је граф чији се скуп чворова $V$ мења током времена. У овом случају, неки чворови могу бити додати или уклоњени. Када се чвор уклони, гране које су инцидентне са тим чвором такође се уклањају.
    \item \textbf{Грана-динамички граф} (енгл. \textit{edge-dynamic graph}) је граф чији се скуп грана $E$ мења током времена. У овом случају, гране могу бити додате или уклоњене из графа.
    \item \textbf{Тежински динамички граф} (енгл. \textit{weight-dynamic graph}) је граф чија се функција $g$ која гранама придружује тежине мења током времена.
    \item \textbf{Потпуно динамички граф} (енгл. \textit{fully dynamic graph}) је граф у којем се све од наведених промена могу дешавати током времена, односно и скуп чворова $V$, и скуп грана $E$, и функција $g$ која гранама придружује тежине могу се мењати током времена.
\end{itemize}

Други начин класификације динамичких графова је на основу начина на који се дешавају промене. На тај начин можемо издвојити следеће врсте динамичких графова \cite{dynamic_graph_processing_survey}:

\begin{itemize}
    \item \textbf{Инкрементални динамички граф} је граф код којег се чворови или гране само додају у граф $G$, али се не уклањају.
    \item \textbf{Декрементални динамички граф} је граф код којег се чворови или гране само уклањају из графа $G$, али се не додају.
    \item \textbf{Потпуно динамички граф} је граф код којег се чворови или гране могу и додавати и уклањати из графа $G$.
    \item \textbf{Динамички граф у моделу тока података} је граф код којег се структура мења током времена путем низа узастопних ажурирања (енгл. \textit{data stream}), при чему се чворови и гране додају или уклањају један по један, а обрада се врши секвенцијално.
\end{itemize}

\section{Математички модел динамичких графова}

Прецизан модел динамичког графа описаћемо математичком структуром. Динамички граф $\mathcal{G}$ је уређена тројка $(\mathcal{V}, \mathcal{E}, \mathcal{T})$, где је $\mathcal{V} = \{V(t), t \in \mathcal{T}\}$ колекција скупова чворова током времена, $\mathcal{E} = \{E(t), t \in \mathcal{T}\}$ колекција скупова грана током времена, а $\mathcal{T}$ временски интервал. За сваки $t \in \mathcal{T}$, дефинишемо \textbf{верзију графа} (енгл. \textit{snapshot}) $G_t = (V(t), E(t))$, односно статички граф који представља стање динамичког графа $\mathcal{G}$ у тренутку $t$ \cite{survey_on_embedding_dynamic_graphs}. Лако се види да овако дефинисан динамички граф подржава све врсте динамичких графова које смо раније навели, јер се скуп чворова $V(t)$ и скуп грана $E(t)$ могу мењати током времена. Ову дефиницију можемо проширити додавањем функције $g_t =\{g(t), t \in \mathcal{T} \}$ која сваком стању динамичког графа придружује функцију тежине у тренутку $t$, чиме добијамо тежински динамички граф $\mathcal{G} = (\mathcal{V}, \mathcal{E}, g_t, \mathcal{T})$.

Иако математички прецизан, овако дефинисан модел динамичког графа није увек погодан за рачунску и алгоритамску примену. Наиме, у многим случајевима, системи који се моделују динамичким графом могу се мењати и неколико пута у секунди, што би захтевало да се комплетна структура графа чува и обрађује за сваки тренутак времена, што је рачунски неизводљиво. Уместо да памтимо комплетну структуру графа за сваки тренутак времена, можемо памтити секвенцу промена које су се десиле у току времена. Такав модел биће представљен у наредном поглављу.

\section{Динамички графови у моделу тока података}

\textbf{Ток података} (енгл. \textit{data stream}) у општем смислу представља секвенцу дигитално кодираних сигнала који се користе за пренос информација \cite{muthukrishnan2005data}. У контексту алгоритама и обраде података, овај појам се апстрахује као низ елемената који пристижу секвенцијално, елемент по елемент.

Улазни ток
\[
S = \langle a_1, a_2, \ldots \rangle
\]
описује еволуцију функције стања
\[
A : \{1,2,\dots,N\} \to \mathbb{R}.
\]
Нека је са \(A[i]\) означено стање функције \(A\) након обраде првих \(i\)
елемената тока. Модели се разликују по томе како елементи \(a_i\)
описују промене функције \(A\) \cite{muthukrishnan2005data}.

У раду \cite{muthukrishnan2005data} издвајају се следећи основни модели:

\begin{itemize}
    \item \textbf{Модел временских серија} (енгл. \textit{Time Series Model}) у којем
    је сваки елемент тока директна вредност, односно важи \(a_i = A[i]\).

    \item \textbf{Модел касе} (енгл. \textit{Cash Register Model}) у којем
    елементи тока представљају позитивна ажурирања облика \(a_i = (j, I_i)\),
    где је \(I_i \ge 0\), при чему важи
    \[
    A_i[j] = A_{i-1}[j] + I_i.
    \]

    \item \textbf{Модел окретнице} (енгл. \textit{Turnstile Model}) у којем
    су дозвољена и позитивна и негативна ажурирања облика \(a_i = (j, U_i)\),
    где је \(U_i \in \mathbb{R}\), при чему важи
    \[
    A_i[j] = A_{i-1}[j] + U_i.
    \]
\end{itemize}

Овај модел се природно проширује на динамичке графове, који се могу
представити као секвенца ажурирања над структуром графа током времена.

Модел временских серија је најједноставнији модел: у њему би сваки елемент тока представљао комплетну структуру графа у одређеном тренутку времена. Овај модел је еквивалентан математичком моделу наведеном у претходном поглављу па неће бити поново разматран.

Модел касе и модел окретнице могу се користити за представљање динамичких графова на следећи начин: сваки елемент тока представља додавање или уклањање једног чвора или једне гране у граф. У моделу касе, дозвољена су само позитивна ажурирања, што значи да се чворови и гране могу само додавати у граф, али не и уклањати. У моделу окретнице, дозвољена су и позитивна и негативна ажурирања, што значи да се чворови и гране могу и додавати и уклањати из графа. У наставку ћемо описати оба модела са фокусом на измене грана, док ћемо скуп чворова сматрати константним. Измене чворова могу се посматрати на исти начин као и измене грана. 

За потребе модела касе, сматраћемо да је сваки елемент тока уређени пар \(e_i=(u, v)\) који представља додавање гране између чворова \(u\) и \(v\). Такав ток, 
\[S = \langle e_1, e_2, \ldots, e_m \rangle\]
дефинише динамички граф \(G = (V, E)\) где је \(V\) фиксан скуп чворова, а \(E={e_1, \dots , e_m}\) скуп грана који се мења током времена додавањем нових грана из тока \cite{graph_stream_algorithms_survey}.  Такође, елементима тока можемо доделити тежине, тако да сваки елемент тока буде уређени пар \((e_i, w(e_i))\) који представља додавање гране \(e_i\) са тежином \(w(e_i)\). На тај начин добијамо тежински динамички граф \cite{graph_stream_algorithms_survey}. На слици \ref{fig:primer_dinamickog_grafa1} приказан је пример конструкције динамичког графа са четири чвора дефинисан током података \(S = \langle (1, 2), (2, 3), (1, 3), (3, 4)\rangle\). Зеленом бојом означене су гране додате у том временском тренутку, а црном бојом означене су гране које су већ биле присутне у графу. 

\begin{figure}[hbt]
\centering
\begingroup
\shorthandoff{"}
\digraph[scale=0.6]{dynamicgraphexample1}{
    rankdir=LR;
    edge [color=black];
    node [shape=circle];
    1 -> 2[style=invis];
    2 -> 3[style=invis];
    1 -> 3[style=invis];
    3 -> 4[style=invis];
}

\digraph[scale=0.6]{dynamicgraphexample2}{
    rankdir=LR;
    edge [color=black];
    node [shape=circle];
    1 -> 2[color=green];
    2 -> 3[style=invis];
    1 -> 3[style=invis];
    3 -> 4[style=invis];
}

\digraph[scale=0.6]{dynamicgraphexample3}{
    rankdir=LR;
    edge [color=black];
    node [shape=circle];
    1 -> 2;
    2 -> 3[color=green];
    1 -> 3[style=invis];
    3 -> 4[style=invis];
}

\digraph[scale=0.6]{dynamicgraphexample4}{
    rankdir=LR;
    edge [color=black];
    node [shape=circle];
    1 -> 2;
    2 -> 3;
    1 -> 3[color=green];
    3 -> 4[style=invis];
}

\digraph[scale=0.6]{dynamicgraphexample5}{
    rankdir=LR;
    edge [color=black];
    node [shape=circle];
    1 -> 2;
    2 -> 3;
    1 -> 3;
    3 -> 4[color=green];
}
\endgroup
\caption{Динамички граф са четири чвора дефинисан током података \(S = \langle (1, 2), (2, 3), (1, 3), (3, 4)\rangle\).}
\label{fig:primer_dinamickog_grafa1}
\end{figure}

Често системи који се моделују и проблеми који се решавају помоћу динамичких графова не маре за претходна стања графа. Очекивања су да се одржавају само информације о тренутном стању графа, али и да су те информације ажурне и да се могу ефикасно обрађивати. Због тога ћемо, за потребе имплементације, у наредном поглављу дефинисати ослабљену верзију динамичког графа, која ће нам омогућити да се фокусирамо на тренутно стање графа и да ефикасно обрађујемо промене које се дешавају током времена.

% Статички и динамички графови